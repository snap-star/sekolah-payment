name: Release & Semantic Versioning

# Trigger the workflow on push to main branch or when manually dispatched
on:
  push:
    branches:
      - master
      - main
  workflow_dispatch:
    inputs:
      version_bump:
        description: 'Version bump type (for manual bump only)'
        required: false
        type: choice
        options:
          - patch
          - minor
          - major
          - prerelease
      prerelease_identifier:
        description: 'Prerelease identifier (only for prerelease bump, e.g. beta, rc)'
        required: false
        type: string

# Permissions required for creating releases and pushing tags
permissions:
  contents: write
  issues: write
  pull-requests: read

env:
  DEFAULT_BRANCH: ${{ github.ref_name }}

jobs:
  release:
    name: Automatic Semantic Release
    runs-on: ubuntu-latest
    if: github.repository_owner == 'snap-star' && github.event_name == 'push'
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true
          
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: 'frontend/sekolahpay-frontend/pnpm-lock.yaml'
          
      - name: Install pnpm
        continue-on-error: true
        run: npm install -g pnpm
        
      - name: Install semantic-release dependencies
        run: |
          npm install -g semantic-release @semantic-release/changelog @semantic-release/git @semantic-release/github @semantic-release/npm conventional-changelog-conventionalcommits
        continue-on-error: true
        
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          coverage: none
        continue-on-error: true
          
      - name: Install semantic-release dependencies
        run: |
          npm install -g semantic-release @semantic-release/changelog @semantic-release/git @semantic-release/github @semantic-release/npm conventional-changelog-conventionalcommits
          
      - name: Install frontend dependencies
        run: |
          cd frontend/sekolahpay-frontend
          pnpm install
          
      - name: Install backend dependencies
        run: |
          cd backend/sekolahpay_server
          composer install --no-dev --optimize-autoloader
          
      - name: Run TypeScript checks (frontend)
        run: |
          cd frontend/sekolahpay-frontend
          pnpm tsc -b
          
      - name: Run Psalm static analysis (backend)
        run: |
          cd backend/sekolahpay_server
          vendor/bin/psalm --show-info=false
        continue-on-error: true
          
      - name: Build frontend for production
        run: |
          cd frontend/sekolahpay-frontend
          pnpm run build
          
      - name: Run semantic-release
        id: release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          npx semantic-release --extends ./semantic-release.config.js
          
      - name: Upload release artifacts
        if: steps.release.outputs.release_tag != ''
        uses: actions/upload-artifact@v4
        with:
          name: sekolahpay-release-${{ steps.release.outputs.release_tag }}
          path: |
            frontend/sekolahpay-frontend/.next/standalone
            frontend/sekolahpay-frontend/.next/static
            backend/sekolahpay-server/public
            backend/sekolahpay-server/app
          if-no-files-found: ignore

  manual-bump:
    name: Manual Version Bump
    runs-on: ubuntu-latest
    if: github.repository_owner == 'snap-star' && github.event_name == 'workflow_dispatch' && github.event.inputs.version_bump != ''
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name: Install semver tool
        run: npm install -g semver
          
      - name: Read current version from CHANGELOG
        id: get-current-version
        run: |
          CURRENT_VERSION=$(grep -m 1 "## \[" frontend/sekolahpay-frontend/CHANGELOG.md | grep -oP '\[\K\d+\.\d+\.\d+')
          echo "current_version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
          echo "Current version: $CURRENT_VERSION"
          
      - name: Bump version
        id: bump-version
        run: |
          if [ "${{ github.event.inputs.version_bump }}" = "prerelease" ]; then
            PREID="${{ github.event.inputs.prerelease_identifier}}"
            if [ -z "$PREID" ]; then PREID="rc"; fi
            NEW_VERSION=$(semver ${{ steps.get-current-version.outputs.current_version }} -i prerelease --preid $PREID)
          else
            NEW_VERSION=$(semver ${{ steps.get-current-version.outputs.current_version }} -i ${{ github.event.inputs.version_bump }})
          fi
          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "New version: $NEW_VERSION"
          
      - name: Update CHANGELOG.md with new version
        run: |
          TODAY=$(date +%Y-%m-%d)
          NEW_VERSION="${{ steps.bump-version.outputs.new_version }}"
          CURRENT_VERSION="${{ steps.get-current-version.outputs.current_version }}"
          sed -i "0,/## \[/s/## \[/## [$NEW_VERSION] - $TODAY\n\n### Summary\nAutomated version bump from $CURRENT_VERSION\n\n---\n\n## [/" frontend/sekolahpay-frontend/CHANGELOG.md
          
      - name: Update version.json with new version
        run: |
          NEW_VERSION="${{ steps.bump-version.outputs.new_version }}"
          node -e "
            const fs = require('fs');
            const data = JSON.parse(fs.readFileSync('./version.json', 'utf8'));
            data.version = '$NEW_VERSION';
            const parts = '$NEW_VERSION'.split('.');
            data.version_code = parseInt(parts[0])*10000 + parseInt(parts[1])*100 + parseInt(parts[2]);
            data.release_date = new Date().toISOString().split('T')[0];
            fs.writeFileSync('./version.json', JSON.stringify(data, null, 2) + '\\n');
          "
          
      - name: Update root package.json
        run: |
          NEW_VERSION="${{ steps.bump-version.outputs.new_version }}"
          node -e "
            const fs = require('fs');
            const data = JSON.parse(fs.readFileSync('./package.json', 'utf8'));
            data.version = '$NEW_VERSION';
            fs.writeFileSync('./package.json', JSON.stringify(data, null, 2) + '\\n');
          "
          
      - name: Update frontend package.json
        run: |
          NEW_VERSION="${{ steps.bump-version.outputs.new_version }}"
          node -e "
            const fs = require('fs');
            const data = JSON.parse(fs.readFileSync('./frontend/sekolahpay-frontend/package.json', 'utf8'));
            data.version = '$NEW_VERSION';
            fs.writeFileSync('./frontend/sekolahpay-frontend/package.json', JSON.stringify(data, null, 2) + '\\n');
          "
          
      - name: Commit and push version bump changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add version.json
          git add package.json
          git add frontend/sekolahpay-frontend/package.json
          git add frontend/sekolahpay-frontend/CHANGELOG.md
          git commit -m "chore: bump version to ${{ steps.bump-version.outputs.new_version }}"
          
      - name: Push changes and create tag
        uses: ad-m/github-push-action@master
        with:
          branch: ${{ github.ref_name }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          tags: true
